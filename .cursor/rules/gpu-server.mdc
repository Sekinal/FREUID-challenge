---
description: Servidor GPU FREUID — SSH y rutas de trabajo
alwaysApply: true
---

# Servidor GPU del equipo

- **SSH:** `ssh root@216.81.248.172 -p 40299`
- **Proyecto:** clonar/trabajar en `/root/freuid` o `/workspace/freuid`
- **Evitar:** `/root/steeleye` (otro proyecto)
- **Datos y embeddings:** en el servidor bajo `data/`, `embeddings/` (gitignored)
- **Credenciales:** `LOCAL_CREDENTIALS.txt` solo en Mac; en servidor usar `~/.kaggle/access_token` local
- Jobs largos: recomendar `tmux`/`screen`
- Detalle: `docs/SERVER.md`
