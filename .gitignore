# ---- Data & large artifacts (never committed; live on the box) ----
data/
embeddings/
*.zip
*.npy

# Large, regenerable split manifests (rebuild via scripts/09_build_splits.py)
artifacts/splits/

# ---- Credentials / secrets ----
.kaggle/
access_token
# LOCAL_CREDENTIALS.txt — solo en el Mac del usuario; nunca commit/push
LOCAL_CREDENTIALS.txt
*.git-credentials
.env

# ---- Python ----
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info/
.venv/
runs/
submissions/
.ipynb_checkpoints/

# ---- OS / editor ----
.DS_Store

# transient validation smoke output (random scorer)
artifacts/validation_smoke.json

# full near-dup pairs are large + regenerable (scripts/05); keep only the summary
artifacts/duplicates.json

# forensic feature matrix (large, regenerable via scripts/14)
artifacts/forensic_features.npz

# spectral mean-spectra (regenerable via scripts/17)
artifacts/spectral_means.npz

# CLIP frozen features (large, regenerable via scripts/19)
artifacts/clip_features.npz

# fusion feature cache (large, regenerable via scripts/24)
artifacts/fusion_features.npz

# ---- experiment logs & box-local regenerables ----
*.log
logs/
models_synth/
docker/weights/
# pseudo-label / synth / extra-data manifests: absolute box paths, regenerable
artifacts/pseudo*.csv
artifacts/soft_pseudo.csv
artifacts/extra_*.csv
artifacts/synth_*.csv
artifacts/fantasyid.csv
# raw OCR dump (large, regenerable via scripts/ocr_build.py)
artifacts/consistency/ocr_raw.jsonl
