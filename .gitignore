# ---- Data & large artifacts (never committed; live on the box) ----
data/
embeddings/
*.zip
*.npy

# Large, regenerable split manifests (rebuild via scripts/09_build_splits.py)
artifacts/splits/

# ---- Credentials / secrets ----
.kaggle/
access_token
# LOCAL_CREDENTIALS.txt — solo en el Mac del usuario; nunca commit/push
LOCAL_CREDENTIALS.txt
*.git-credentials
.env

# ---- Python ----
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info/
.venv/
runs/
submissions/
.ipynb_checkpoints/

# ---- OS / editor ----
.DS_Store

# ---- Docker build context weights (from GitHub Release; never committed) ----
docker/weights/

# ---- Editor/infra local (contiene coordenadas de servidores) ----
.cursor/
docs/SERVER.md
